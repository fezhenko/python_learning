def my_func(param1): # в скобках можно указать параметры
    func = sum(param1) / len(param1)
    return func

print(my_func([1,2,3,4]))

def calculate_length(lst):
    x = len(lst)
    return x
lst = [1,2,3,4,5]